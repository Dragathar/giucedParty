{
  "name": "giucedparty-server",
  "version": "1.0.0",
  "description": "giucedParty WebSocket relay server for synced video playback + chat",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
